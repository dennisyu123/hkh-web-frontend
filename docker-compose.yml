version: '3.8'

services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./:/app:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8081:80"
    restart: unless-stopped
